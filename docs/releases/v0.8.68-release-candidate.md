# CodeWhale v0.8.68 Release-Candidate Ledger

> **Authoritative status as checked 2026-07-14:** release source under final
> verification; not yet publicly published. Update this ledger when the
> candidate PR, verification,
> tag, or package state changes. Historical trackers and handoffs are planning
> evidence only.

## Candidate identity

| Surface | Current state |
|---|---|
| Workspace Cargo version | `0.8.68` |
| npm wrapper manifest | `codewhale@0.8.68` |
| Candidate branch | `codex/v0868-cursor-qa-fixes` |
| Integration baseline | `cc540fc6985d01d1ac65b33a2d08143fc6d78a45` (before final release-note synchronization) |
| GitHub `v0.8.68` tag | Absent |
| GitHub `v0.8.68` release | Absent |
| crates.io `codewhale-*` release set | `0.8.68` absent; published crates remain at `0.8.67`, while `codewhale-lane` has no public version yet |
| npm `codewhale` | `0.8.68` absent; latest is `0.8.67` |

`scripts/release/check-published.sh 0.8.68`, the GitHub release API, and the
remote tag list were checked on 2026-07-14. A dated changelog section and local
version metadata do not make a public release.

## Acceptance accounting

GitHub issues stay open until the candidate is pushed, reviewed, and the issue
owner accepts the evidence. As of the 2026-07-14 refresh, #4333, #4355, #4356,
#4357, #4358, and #4359 are closed from reviewed evidence. The following work
is not honestly closed by this candidate audit:

- [#4175](https://github.com/Hmbown/CodeWhale/issues/4175),
  [#4177](https://github.com/Hmbown/CodeWhale/issues/4177),
  [#4178](https://github.com/Hmbown/CodeWhale/issues/4178), and
  [#4179](https://github.com/Hmbown/CodeWhale/issues/4179) still require
  completed live Fleet role-resolution, stopship-dogfood, and gate/handoff
  receipts before their broader acceptance can close.
- [#4236](https://github.com/Hmbown/CodeWhale/issues/4236) and
  [#4242](https://github.com/Hmbown/CodeWhale/issues/4242) retain official
  Termux scope and real-device runtime QA.

The same refresh found seven open pull requests and no safe release merge:
#4370 is a conflicting, provider-specific slice of unfinished generic catalog
work; #4367 is replaced by the bounded off-thread mention fix in this
candidate; and #4293, #4199, #4087, #4086, and #3781 remain explicitly
deferred, draft, conflicting, or outside v0.8.68 scope. These decisions must be
rechecked immediately before the release PR merges.

Provider-route degradation during Fleet dogfood and unavailable real-device
Termux evidence must be reported as external/manual gaps, not silently converted
to passes.

## Verification and publication gates

Candidate metadata alignment and Cargo publish/package dry-runs have passed in
the current audit. Before calling v0.8.68 ready or published, complete all of
the following against the final commit:

1. Run formatting, targeted regression tests, workspace/release checks, and a
   locked release build; record any platform or provider limitation.
2. Install the final local binaries and manually exercise the underwater shell,
   settings/permission controls, links, composer, worker transcripts, Workflow
   controls, cancellation, and reduced-motion paths.
3. Push the candidate branch, open and review a PR against `main`, and update
   the issue accounting above from reviewed evidence.
4. Merge to `main`, create signed/verified `v0.8.68`, and let the release
   workflow produce and verify checksummed platform assets.
5. Publish the ordered `codewhale-*` crate set and npm `codewhale@0.8.68`, then
   rerun `scripts/release/check-published.sh 0.8.68` until it passes.
6. Confirm the GitHub release, updater/install paths, Cargo installs, npm
   wrapper, and package registries all resolve the same final build.

Do not install or publish the local experimental Z.AI plugin draft as part of
v0.8.68; it is outside this candidate and has not passed the release gates.
